// Minimal stub for atomic_queue.h
// The original project expects an `atomic_queue` submodule. This stub allows
// the code to compile when that dependency is missing. It intentionally
// provides no functionality — replace with the real library for production use.

#pragma once

namespace atomic_queue_stub
{
    // placeholder types or functions can be added here if needed
}
